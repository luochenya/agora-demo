const state = {
  // agora-appId
  agora_appid: process.env.VUE_APP_AGORA_APPID,
}

const mutations = {
  
}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}